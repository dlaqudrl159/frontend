import React, { memo } from "react";
import { Link } from "react-router-dom";

const Header = memo((props) => {
  
  return (
    <div style={{height : 'auto' , width : '500px', backgroundColor :'blcak' ,  zIndex : '2'}}>
     <div style={{height : '10dvh'}}></div>
    </div>
  )
})
export default Header;