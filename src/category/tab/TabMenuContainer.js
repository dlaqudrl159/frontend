import { Box } from "@mui/material";
import React, {memo} from "react";

const TabMenuContainer = () => {
    return (
        <Box sx={{
            width: '100%', height: '20%', display: 'flex', justifyContent: "space-between",
            backgroundColor: "white",
            alignItems: "center",
            pointerEvents: 'auto'
          }}>
            
          </Box>
    )
}

export default TabMenuContainer;